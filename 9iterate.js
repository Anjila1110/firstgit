//Write a program that iterates over an array of names and logs each name to the console with its index. 

const names = ["Anji", "Juna", "Alijna", "Diana"];

names.forEach((name,index) => {
  console.log(name,index);
});